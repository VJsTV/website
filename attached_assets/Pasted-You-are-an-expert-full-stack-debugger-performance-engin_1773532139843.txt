You are an expert full-stack debugger, performance engineer, and front-end QA specialist. Your job is to ANALYZE, FIX, OPTIMIZE, DEBUG, and TEST the entire VJs TV website in this Replit (the premium VJ platform with neon cyberpunk aesthetic, canvas particle background, Tailwind, smooth-scroll sections, Live Now player, VJ grid, Projects grid, Technology section, and Sponsor modals).

Current files: index.html, style.css (Tailwind), script.js

**Step-by-step workflow you MUST follow** (do all of them):

1. **Error Detection & Fixing**
   - Scan for JavaScript console errors, unclosed tags, invalid Tailwind classes, broken modals, non-working buttons, or canvas errors.
   - Fix every bug you find (especially particle background, modals, smooth scrolling, video placeholders, hover glows, and mobile menu).

2. **Performance Optimization**
   - Make the site load instantly (lazy-load images/videos, optimize canvas particles so they never drop FPS, debounce mouse events).
   - Minimize Tailwind classes, remove unused CSS, add proper meta tags for fast rendering.
   - Ensure Lighthouse score would be 95+ (Core Web Vitals: LCP, FID, CLS).

3. **Responsiveness & Cross-Device Testing**
   - Test and fix on mobile, tablet, and desktop (especially the video grids, modals, and navbar).
   - Make sure the canvas background scales perfectly and doesn’t lag on phones.

4. **Accessibility & SEO Polish**
   - Add proper ARIA labels, alt texts, keyboard navigation, focus states.
   - Add Open Graph meta tags, title, description, and basic schema for “VJs TV – Live Visual Culture”.

5. **Visual & UX Polish**
   - Strengthen neon glows (text-shadow + box-shadow) if they look weak.
   - Ensure all hover animations and modal transitions are buttery smooth.
   - Keep the exact dark cyberpunk palette (#050505 background, cyan #00f9ff, magenta #ff00aa, violet #9d00ff) and VJ energy.

6. **Testing**
   - Simulate “Watch Live Now”, clicking Sponsor category cards, filtering VJs, and scrolling.
   - Confirm everything works perfectly before outputting.

**Output format**:
- First, list every fix and optimization you made (bullet points).
- Then give the COMPLETE updated code for:
  - index.html (full file)
  - style.css (full file)
  - script.js (full file)
- End with: “Site is now error-free, optimized, and production-ready. Run it and test!”

Do not remove any sections (Live, VJs, Projects, Technology, Sponsors). Do not change the aesthetic. Make it even more premium and flawless.